<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyforge.system.mapper.achievement.AchievementMapper">
    <resultMap id="AchievementResultMap" type="com.studyforge.system.entity.achievement.Achievement">
        <id column="achievement_id" property="achievementId"/>
        <result column="achievement_code" property="achievementCode"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="icon" property="icon"/>
        <result column="category" property="category"/>
        <result column="requirement_type" property="requirementType"/>
        <result column="requirement_value" property="requirementValue"/>
        <result column="reward_points" property="rewardPoints"/>
        <result column="sort_no" property="sortNo"/>
        <result column="status" property="status"/>
        <result column="created_time" property="createdTime"/>
    </resultMap>

    <sql id="AchievementColumns">
        achievement_id,
        achievement_code,
        name,
        description,
        icon,
        category,
        requirement_type,
        requirement_value,
        reward_points,
        sort_no,
        status,
        created_time
    </sql>

    <select id="selectById" parameterType="long" resultMap="AchievementResultMap">
        SELECT <include refid="AchievementColumns"/>
        FROM achievements
        WHERE achievement_id = #{achievementId}
    </select>

    <select id="selectByCode" parameterType="string" resultMap="AchievementResultMap">
        SELECT <include refid="AchievementColumns"/>
        FROM achievements
        WHERE achievement_code = #{achievementCode}
    </select>

    <select id="selectAll" resultMap="AchievementResultMap">
        SELECT <include refid="AchievementColumns"/>
        FROM achievements
        <if test="status != null and status != ''">
            WHERE status = #{status}
        </if>
        ORDER BY sort_no ASC
    </select>

    <select id="selectByCategory" resultMap="AchievementResultMap">
        SELECT <include refid="AchievementColumns"/>
        FROM achievements
        WHERE category = #{category}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY sort_no ASC
    </select>
</mapper>
